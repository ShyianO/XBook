export interface IConfiguration {
  name: string;
  title: string;
  description?: string;
  phoneNumber: string;
  email?: string;
  address: string;
  city: string;
  state?: string;
  postalCode: string;
  objectId?: string;
}
